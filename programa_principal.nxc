  #include <"navegacao.h">
  #include <"cor.h">

  task main() {
    Precedes(tPrinta_cor,tGira_180);
  }
  /*
  task main() {
    SetSensorLowspeed(IN_4);
    OnRev(OUT_AC, 75);
    until(SensorUS(IN_4) < 15);
    Float(OUT_AC);
    gira_180();
  }
  */
