  #include <e_preto.h>
  #include <encontra_cor.h>

  task main() {
    while(true) {
      if (e_preto()){
          fim_linha();
        }
        else if (encontra_cor()){
          explora();
        }
    }
  }
